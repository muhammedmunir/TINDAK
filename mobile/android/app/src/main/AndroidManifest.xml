<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- M5b: the first network permission TINDAK requests. Used only for
         Supabase Auth (email OTP) and Memory sync for a signed-in user
         (docs/13_API.md). A guest never contacts the cloud (PD-001), and
         cleartext HTTP stays disabled below. Changes the Play Data Safety
         declaration (docs/14_M5B_RECONCILIATION.md section 5). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Security posture: see docs/12_SECURITY.md sections 4 and 7 (ADR-021).
         allowBackup=false and the extraction rules stop the local database
         being copied into cloud backup or an adb backup, which is the
         realistic path for private memories to leave the device. -->
    <application
        android:label="TINDAK"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- The product's entry point (ADR-003). Explicit user share only:
                 no clipboard monitoring, no background reading (ADR-004).

                 text/plain is the whole of V1's input scope (PRD section 2).
                 Nothing else is declared, so the Android sharesheet does not
                 offer TINDAK for images, files or multiple items. An intent
                 that arrives anyway is rejected in MainActivity. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
